const name = prompt('What is your name?');

// In order to use template literals, apply back ticks (`) instead of quotations (" " or '') 

const message = `Hello ${name}. Welcome to my tech site.
I'm happy that you came by to visit, ${name}.
Please come again to observe better updates in the future!`;

console.log(message);